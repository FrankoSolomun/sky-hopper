using UnityEngine;

public class PlatformItemAttachment : MonoBehaviour
{
    // This flag is false by default.
    // When an item (star, power-up, or obstacle) is attached, set it to true.
    public bool hasItem = false;
}